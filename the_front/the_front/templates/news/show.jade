extends layout

block content
  .col-sm-12
    h1 #{news.name} (#{news.date})
    p=news.text
  .row
    br
  .col-sm-10.col-sm-offset-1
    {% for image in news_images %}
    .col-sm-4
      img(src="#{image.thumb()}", title="#{image.name}")
    {% endfor %}
