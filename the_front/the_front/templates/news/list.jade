extends layout

block content
  {% for article in news %}
  .col-sm-2
      a(href='#{article.get_absolute_url()}') #{article.name}
        p #{article}
        img(src="#{article.thumb()}")
  {% endfor %}
