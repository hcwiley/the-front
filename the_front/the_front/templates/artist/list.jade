extends layout

block content
  .col-sm-10
    h1.pull-right artists
  .col-sm-12
    {% for artist in artists %}
    .col-sm-3
      a(href='#{artist.get_absolute_url()}')
        p #{artist}
        img.img-thumbnail(src="#{artist.thumb()}")
    {% endfor %}
