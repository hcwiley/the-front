extends layout

block content
  .col-sm-12
    h1 #{artist.name}
    h4 collection of work
  .row
    br
  .col-sm-10.col-sm-offset-1
    {% for image in artist_images %}
    .col-sm-4
      p #{image.name}
      img(src="#{image.thumb()}")
    {% endfor %}
